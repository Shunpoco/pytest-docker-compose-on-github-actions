version: "2"
services:
  httpbin:
    image: "kennethreitz/httpbin"
    ports:
    - "8080:80"
    # network_mode: "__NETWORK_ID__"

networks:
  default:
    name: "__NETWORK_ID__"
    external: true
